# Legacy Code

This directory contains older extraction and intervention scripts that are not part of the current main pipeline.

They are kept for reference and backward compatibility with older notebooks/tests, but the active CSQA trace-feature direction lives in:

- `src/cli/extract_csqa_trace_feature_tables.py`
- `src/cli/run_csqa_adaptive_contrastive_pipeline.py`
